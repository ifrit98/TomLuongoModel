# V1 baseline outputs

These files are retained only to reproduce/inspect the public-data findings that motivated V2. The V1 `event_study.csv` is intentionally omitted because V2 fixes a windowing/return-aggregation issue discovered during review. Run the V2 harness to regenerate event results correctly.
